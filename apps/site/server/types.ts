import type {
  Brand,
  Collection,
  Product,
  Retailer,
  Variant,
} from "@repo/types";

// Define server-specific types here
export type ServerBrand = Brand;
export type ServerCollection = Collection;
export type ServerProduct = Product;
export type ServerVariant = Variant;
export type ServerRetailer = Retailer;
