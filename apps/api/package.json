{
    "name": "@repo/api",
    "type": "module",
    "private": true,
    "scripts": {
        "ui:build": "vite build",
        "ui:dev": "vite --host=localhost",
        "dev": "frankenphp php-server -l localhost:8000 -r public",
        "prepare": "composer install && composer prepare"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.2.4"
    }
}
